# NCBCNet
#
# 架构边界契约说明：
# - 曾尝试用 import-linter 守护「api 不得导入业务 models」，
#   但本项目应用均为顶层平级包（api/ article/ file_save/ ... 并列于仓库根目录），
#   不存在 import-linter 所需的「根包」（root_package），无法适配。
# - 该约束已由 api/tests.py 的 ApiBoundaryTests（AST 扫描 import）强制执行，
#   并在 CI 的 Django 测试中运行。
